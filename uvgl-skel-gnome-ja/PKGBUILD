# Maintainer: Hayate Nakamura (is01.njb at gmail dot com)
pkgname=("uvgl-skel-gnome-ja")
_pkgname=("uvgl-skel")
destname="/etc"
pkgver=23.03
pkgrel=2
pkgdesc="Settings for Gunivalent GNU/Linux"
arch=('any')
url="https://github.com/jin-asanami/uvgl-skel"
license=('GPL3')

depends=('gnome-terminal' 'nautilus')

optdepends=('yay: an AUR helper'
            'unifetch: screenfetch utility'
            'cups: printing support'
            'system-config-printer: CUPS status applet')
conflicts=()
provides=("${pkgname}")
options=(!strip !emptydirs)
source=("${_pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/v${pkgver}.tar.gz"
	"user")
sha256sums=('75f36f4c6f8a3707d3539561362815246bf310cc09166a47705d98ed1f6a3bdb'
	'ae8cf40879a90ff860361fc1a5d620b0f08c2cb9841a779321e5552caa2e76d8')

package() {
	install -dm755 ${pkgdir}${destname}
	cp -r ${srcdir}/${_pkgname}-${pkgver}/gnome/ja${destname}/* ${pkgdir}${destname}
	cp -f ${srcdir}/user ${pkgdir}${destname}/skel/.config/dconf/
}
