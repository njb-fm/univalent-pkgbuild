# Maintainer: Hayate Nakamura (is01.njb at gmail dot com)
pkgname=("uvgl-skel-lxqt-kamu-en")
destname="/etc"
pkgver=23.01
pkgrel=4
pkgdesc="Settings for Kamuvalent GNU/Linux"
arch=('any')
url="https://github.com/jin-asanami"
license=('GPL3')
makedepends=('git')

depends=('qterminal' 'pcmanfm-qt')

optdepends=('yay: an AUR helper'
            'unifetch: screenfetch utility'
            'cups: printing support'
            'system-config-printer: CUPS status applet')
conflicts=()
provides=("${pkgname}")
options=(!strip !emptydirs)
source=(uvgl-skel::"git+${url}/uvgl-skel")
sha256sums=('SKIP')

package() {
	install -dm755 ${pkgdir}${destname}
	cp -r ${srcdir}/uvgl-skel/lxqt-kamu/en${destname}/* ${pkgdir}${destname}
}
